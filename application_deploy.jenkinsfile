pipeline {
    agent any

    stages {
        stage('execute Ansible') {
            steps {
                ansiblePlaybook disableHostKeyChecking: true, installation: 'Ansible', inventory: 'dev.inv', playbook: 'kubectl_ansible.yml'
            }
        }
    }
}
